name: Deploy Frontend

on:
  workflow_dispatch:

  push:
    branches:
      - main
      - dev

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        run: echo "workflow running"
